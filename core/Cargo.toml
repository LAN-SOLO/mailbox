[package]
name = "mailbox-core"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "mailbox core: mail domain — addresses, folders, MIME helpers, local cache"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
mail-parser = "0.11"
base64 = "0.22"
